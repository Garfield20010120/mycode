#include<stdio.h>
//22.4.18-操作符-操作符属性（优先级、结合性、是否控制求值顺序）决定的问题表达式 
int main()
{
	int a = 1;
	int b = a + ++a;//操作符的优先级只能决定++a优先于+进行，但无法得知+的左操作数获取是在++a前还是后
	int c = 1;
	int d = (++c)+(++c)+(++c);//cs2013输出为12 
	printf("b=%d\nd=%d",b,d); //有歧义不同编译器编译结果不一样 应避免 
	return 0;
}
